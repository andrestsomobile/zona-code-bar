package administracion.action;import java.io.IOException;import javax.servlet.ServletException;import java.sql.SQLException; import org.apache.struts.action.ActionMessage;  import org.apache.struts.action.ActionMessages; import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import javax.servlet.http.HttpSession;import java.util.*;import administracion.control.*;import administracion.entity.*;import administracion.form.*;import org.apache.struts.action.*;  public final class permisoAction  extends Action{
			 public ActionForward execute(ActionMapping mapping,	ActionForm form, 			HttpServletRequest request,	HttpServletResponse response)			throws IOException, ServletException {
permisoForm _form = (permisoForm)form;  gstpermiso control = new gstpermiso(); String opcion = request.getParameter("opcion");  opcion = opcion==null || ( opcion!=null && opcion.equals("")) ? "crear" : opcion; 
//OPCION DE set:  if(opcion.equals("set")) {  permiso entity = control.getpermiso(request.getParameter("codsx")); permisoForm  temp  = new permisoForm();  temp.setopcion("update"); temp.llenar(entity);  request.setAttribute("permisoForm", temp);  return mapping.findForward("permiso_datos");  }  String mensaje = "";  //opcion de CREAR  if(opcion.equals("crear")) {  String pergrupo = _form.getpergrupo();   String perapp = _form.getperapp();   String permodulo = _form.getpermodulo();   String peracceso = _form.getperacceso();  
  try {  control.crearpermiso(pergrupo,perapp,permodulo,peracceso);  mensaje = "permiso Creado con Exito";    } catch (SQLException e) {   e.printStackTrace();  mensaje = "No se pudo Crear el permiso: <br> " + e.getLocalizedMessage();   }  }  // Opcion de update  if(opcion.equals("update")) {   try {   control.updatepermiso(_form);   mensaje = "permiso Actualizado con Exito";    } catch (SQLException e) { e.printStackTrace();   mensaje = "No se pudo Actualizar el permiso: <br> " + e.getLocalizedMessage();  }  }  // Opcion de eliminar  if(opcion.equals("delete")) { String codsx = request.getParameter("codsx");  try {   control.eliminar(codsx);   mensaje = "permiso Eliminado con Exito";    } catch (SQLException e) { e.printStackTrace();   mensaje = "No se pudo Eliminar el permiso: <br> " + e.getLocalizedMessage();  }  }  ActionMessages e = getErrors(request);  e.add("general", new ActionMessage(mensaje, false)); addErrors(request, e);  return mapping.findForward("permiso");    } }