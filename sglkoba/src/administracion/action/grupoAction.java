package administracion.action;import java.io.IOException;import javax.servlet.ServletException;import java.sql.SQLException; import org.apache.struts.action.ActionMessage;  import org.apache.struts.action.ActionMessages; import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import javax.servlet.http.HttpSession;import java.util.*;import administracion.control.*;import administracion.entity.*;import administracion.form.*;import org.apache.struts.action.*;  public final class grupoAction  extends Action{
			 public ActionForward execute(ActionMapping mapping,	ActionForm form, 			HttpServletRequest request,	HttpServletResponse response)			throws IOException, ServletException {
grupoForm _form = (grupoForm)form;  gstgrupo control = new gstgrupo(); String opcion = request.getParameter("opcion");  opcion = opcion==null || ( opcion!=null && opcion.equals("")) ? "crear" : opcion; 
//OPCION DE set:  if(opcion.equals("set")) {  grupo entity = control.getgrupo(request.getParameter("codsx")); grupoForm  temp  = new grupoForm();  temp.setopcion("update"); temp.llenar(entity);  request.setAttribute("grupoForm", temp);  return mapping.findForward("grupo_datos");  }  String mensaje = "";  //opcion de CREAR  if(opcion.equals("crear")) {  String gnombre = _form.getgnombre();  
  try {  control.creargrupo(gnombre);  mensaje = "grupo Creado con Exito";    } catch (SQLException e) {   e.printStackTrace();  mensaje = "No se pudo Crear el grupo: <br> " + e.getLocalizedMessage();   }  }  // Opcion de update  if(opcion.equals("update")) {   try {   control.updategrupo(_form);   mensaje = "grupo Actualizado con Exito";    } catch (SQLException e) { e.printStackTrace();   mensaje = "No se pudo Actualizar el grupo: <br> " + e.getLocalizedMessage();  }  }  // Opcion de eliminar  if(opcion.equals("delete")) { String codsx = request.getParameter("codsx");  try {   control.eliminar(codsx);   mensaje = "grupo Eliminado con Exito";    } catch (SQLException e) { e.printStackTrace();   mensaje = "No se pudo Eliminar el grupo: <br> " + e.getLocalizedMessage();  }  }  ActionMessages e = getErrors(request);  e.add("general", new ActionMessage(mensaje, false)); addErrors(request, e);  return mapping.findForward("grupo");    } }