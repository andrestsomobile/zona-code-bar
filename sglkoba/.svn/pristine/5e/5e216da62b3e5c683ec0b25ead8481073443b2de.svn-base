package maestro.action;import java.io.IOException;import javax.servlet.ServletException;import java.sql.SQLException;import org.apache.struts.action.ActionMessage;import org.apache.struts.action.ActionMessages;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import javax.servlet.http.HttpSession;import java.util.*;import maestro.control.*;import maestro.entity.*;import maestro.form.*;import org.apache.struts.action.*;public final class empleadoAction extends Action {	public ActionForward execute(ActionMapping mapping, ActionForm form,			HttpServletRequest request, HttpServletResponse response)			throws IOException, ServletException {		empleadoForm _form = (empleadoForm) form;		gstempleado control = new gstempleado();		String opcion = request.getParameter("opcion");		opcion = opcion == null || (opcion != null && opcion.equals("")) ? "crear"				: opcion;		// OPCION DE set:		if (opcion.equals("set")) {			empleado entity = control					.getempleado(request.getParameter("codsx"));			empleadoForm temp = new empleadoForm();			temp.setopcion("update");			temp.llenar(entity);			request.setAttribute("empleadoForm", temp);			return mapping.findForward("empleado_datos");		}		String mensaje = "";		// opcion de CREAR		if (opcion.equals("crear")) {			String empnombre = _form.getempnombre();			String empcedula = _form.getempcedula();			String empmail = _form.getempmail();			String empcargo = _form.getempcargo();			try {				control.crearempleado(empnombre, empcedula, empmail, empcargo);				mensaje = "empleado Creado con Exito";			} catch (SQLException e) {				e.printStackTrace();				mensaje = "No se pudo Crear el empleado: <br> "						+ e.getLocalizedMessage();			}		}		// Opcion de update		if (opcion.equals("update")) {			try {				control.updateempleado(_form);				mensaje = "empleado Actualizado con Exito";			} catch (SQLException e) {				e.printStackTrace();				mensaje = "No se pudo Actualizar el empleado: <br> "						+ e.getLocalizedMessage();			}		}		// Opcion de eliminar		if (opcion.equals("delete")) {			String codsx = request.getParameter("codsx");			try {				control.eliminar(codsx);				mensaje = "empleado Eliminado con Exito";			} catch (SQLException e) {				e.printStackTrace();				mensaje = "No se pudo Eliminar el empleado: <br> "						+ e.getLocalizedMessage();			}		}		ActionMessages e = getErrors(request);		e.add("general", new ActionMessage(mensaje, false));		addErrors(request, e);		return mapping.findForward("empleado");	}}